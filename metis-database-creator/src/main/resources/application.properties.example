#Specify the type of the database to be created RECORD_REDIRECT, RECORD, METIS_CORE
creation.database.type=
#Set to true if a drop of the existing database should occur first
database.drop.first=

#Truststore
truststore.path=
truststore.password=

#Socks Proxy
socks.proxy.enabled=
socks.proxy.host=
socks.proxy.port=
socks.proxy.username=
socks.proxy.password=


#Mongo
mongo.hosts=
mongo.port=
mongo.authentication.db=
mongo.username=
mongo.password=
mongo.enableSSL=
#Can be a coma separated value so that multiple similar dbs can created at once
mongo.db=